<nav class="navbar navbar-expand-lg navbar-glass" id="mainNavbar">
    <div class="container">
        <a class="navbar-brand" href="/">
            <span class="brand-icon"><i class="bi bi-tools"></i></span>
            Smart Helper
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <i class="bi bi-list"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-lg-center gap-1">
                <% if (typeof user !=='undefined' && user) { %>
                    <% if (user.role==='helper' ) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/helper/dashboard"><i class="bi bi-grid me-1"></i>Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/helper/booking-request"><i
                                    class="bi bi-bell me-1"></i>Requests</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/helper/history"><i
                                    class="bi bi-clock-history me-1"></i>History</a>
                        </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/user/home"><i class="bi bi-house-door me-1"></i>Home</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/user/booking"><i
                                        class="bi bi-calendar-check me-1"></i>Book</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/user/dashboard"><i class="bi bi-grid me-1"></i>Dashboard</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/user/profile"><i class="bi bi-person me-1"></i>Profile</a>
                            </li>
                            <% } %>
                                <li class="nav-item ms-lg-2">
                                    <a class="nav-link btn-nav" href="#" onclick="logout(); return false;">
                                        <i class="bi bi-box-arrow-right me-1"></i>Logout
                                    </a>
                                </li>
                                <% } else { %>
                                    <li class="nav-item">
                                        <a class="nav-link" href="/">Home</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link" href="/login">Login</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link" href="/helper/login"><i class="bi bi-tools me-1"></i>Helper
                                            Login</a>
                                    </li>
                                    <li class="nav-item ms-lg-1">
                                        <a class="nav-link btn-nav" href="/register">Get Started</a>
                                    </li>
                                    <% } %>
            </ul>
        </div>
    </div>
</nav>

<script>
    // Shrink navbar on scroll
    window.addEventListener('scroll', () => {
        const navbar = document.getElementById('mainNavbar');
        if (window.scrollY > 50) {
            navbar.classList.add('scrolled');
        } else {
            navbar.classList.remove('scrolled');
        }
    });

    function logout() {
        fetch('/api/auth/logout', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' }
        })
            .then(() => window.location.href = '/login')
            .catch(err => console.error('Logout error:', err));
    }
</script>